{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/elo_graphql/elo_graphql.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}